server:
  port: 8087

spring:
  application:
    name: notification-service
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:vandiem2004@gmail.com}
    password: ${SMTP_PASSWORD:scwr fssj gbhx uixp}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

services:
  order-service:
    url: ${ORDER_SERVICE_URL:http://localhost:8083/api/v1/orders}
  auth-service:
    url: ${AUTH_SERVICE_URL:http://localhost:8081/api/v1/auth}
