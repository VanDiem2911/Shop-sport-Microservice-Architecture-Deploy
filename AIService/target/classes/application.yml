server:
  port: 8085
  address: 0.0.0.0

spring:
  application:
    name: ai-service

gemini:
  api-key: ${GEMINI_API_KEY:}
  base-url: https://generativelanguage.googleapis.com/v1beta
  model: ${GEMINI_MODEL:gemini-2.5-flash}

product:
  service-url: ${PRODUCT_SERVICE_URL:http://product-service:8082/api/v1/products}
